import { StyleSheet } from "react-native-web";

const styles = StyleSheet.create({
    title : {
        fontSize: 24,
        marginBottom: 16,
        fontWeight: "bold",
    },
    });

    export default styles;